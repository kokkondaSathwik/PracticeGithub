- git and github is used to track the history of our project

- git log will give the history of our changes and commits

- git remote add origin (our github repo .git link)

- git remote -v (it gives the github links that are attached to our project folder)

- first time we need to use git push origin master

-branching

